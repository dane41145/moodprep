# MoodPrep repository instructions

Before making any change, read `memory.md` in full and treat its recorded behaviors as product requirements.

Do not remove, hide, weaken, or replace a recorded behavior without explicit user confirmation. If two behaviors conflict, ask the user which should take precedence before editing.

After an approved lasting behavior is added or changed, update `memory.md`. Run the regression checks documented there before handoff.

